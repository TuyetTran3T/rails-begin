%p#notice= notice
%h1 Vessels
%table.table-of-contents
  %thead
    %tr
      %th Status
      %th Mmsi
      %th Imo
      %th Name
      %th Callsign
      %th Vessel type
      %th Vessel class
      %th Company
      %th Line
      %th Raw data
      %th Slug
      %th Active
      %th{:colspan => "3"}
  %tbody
    - @vessels.each do |vessel|
      %tr
        %td= vessel.status
        %td= vessel.mmsi
        %td= vessel.imo
        %td= vessel.name
        %td= vessel.callsign
        %td= vessel.vessel_type
        %td= vessel.vessel_class
        %td= vessel.company_id
        %td= vessel.line_id
        %td= vessel.raw_data
        %td= vessel.slug
        %td= vessel.active
        %td= link_to 'Show', vessel
        %td= link_to 'Edit', edit_vessel_path(vessel)
        %td= link_to 'Destroy', vessel, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Vessel', new_vessel_path
